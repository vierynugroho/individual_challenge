{
	"name": "backend",
	"version": "1.0.0",
	"description": "backend - individual challenge chapter 6 by viery",
	"main": "index.js",
	"engines": {
		"node": "18.x"
	},
	"scripts": {
		"dev": "node index.js",
		"migrate": "npx prisma migrate dev",
		"migrate-refresh": "npx prisma migrate reset",
		"vercel-build": "prisma generate && prisma migrate deploy && next build"
	},
	"keywords": [
		"backend",
		"individual",
		"challenge",
		"chapter6",
		"vierynugroho",
		"express",
		"react"
	],
	"author": "Viery Nugroho",
	"license": "ISC",
	"devDependencies": {
		"nodemon": "^3.1.0"
	},
	"dependencies": {
		"@prisma/client": "^5.13.0",
		"cors": "^2.8.5",
		"dotenv": "^16.4.5",
		"express": "^4.19.2",
		"http-errors": "^2.0.0",
		"joi": "^17.13.0",
		"prisma": "^5.13.0"
	}
}
